# ICCAD 2026 FloorSet Challenge - Contest Requirements
# Mirrors IntelLabs/FloorSet/iccad2026contest/requirements.txt, plus the
# packages the Beta Submission Guidelines (Section 2, Case A) state are
# pre-installed in the official evaluation environment:
#   numpy, torch, scipy, numba, tqdm, shapely, threadpoolctl
# Install: pip install -r requirements.txt

torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
numba>=0.58.0
shapely>=2.0.0
threadpoolctl>=3.1.0
matplotlib>=3.7.0
tqdm>=4.60.0
requests>=2.28.0
